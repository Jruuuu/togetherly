export const APPROVAL_TIME_LIMIT_HOURS = 48;
export const DEFAULT_REMINDER_HOURS = 24;
export const DEFAULT_MIN_NOTICE_HOURS = 24;
export const DEFAULT_LINK_EXPIRATION_DAYS = 30;

export const DATE_NIGHT_STATUS = {
  OPEN: 'open',
  FILLED: 'filled',
  CANCELLED: 'cancelled',
} as const;

export const VOLUNTEER_STATUS = {
  PENDING: 'pending',
  APPROVED: 'approved',
  REJECTED: 'rejected',
} as const;

